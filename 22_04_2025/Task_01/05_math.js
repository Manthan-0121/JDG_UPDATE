
export function add(a, b) {
    return a + b;
}

export function subtract(a, b) {
    return a - b;
}

export default function multiply(a, b) {
    return a * b;
}


// only one default export is allowed in a module, so if you want to use default export, you can only have one default export in a file.
// export default function division(a, b) {
//     return a / b;
// }
